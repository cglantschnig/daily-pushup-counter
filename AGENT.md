# Agent Notes

- If you change anything in the Convex database layer, rerun `pnpm convex:dev` before finishing.
- This repo uses `pnpm convex:dev` to sync Convex functions, schema changes, and generated types in `convex/_generated/`.
